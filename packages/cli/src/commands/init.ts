/**
 * Stub for the `vibesafe init` command.
 * Will scaffold vibesafe.config.js in a later sprint.
 */
export async function initCommand(): Promise<void> {
  console.log('init command — not yet implemented');
}
