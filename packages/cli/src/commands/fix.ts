/**
 * Stub for the `vibesafe fix` command.
 * Will run scan + LLM fix generation in a later sprint.
 */
export async function fixCommand(_path: string): Promise<void> {
  console.log('fix command — not yet implemented');
}
